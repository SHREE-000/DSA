// check the string contians A to Z and a to z
// longest repeated frequency of alphabet
// flat method 

const arr = [2, 4, 2, 4, 22, 534, 2, 54, 232, 53];

const sortArr = (arr) => {
    for(let i = 0; i < arr.length; i ++) {
        for(let j = 0; j < arr.length; j ++) {
            if(arr[i]){}
        }
    }
};
